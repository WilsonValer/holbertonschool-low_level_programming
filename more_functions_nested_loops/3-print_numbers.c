#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int n = 0;

	while (n < 10)

	{
		printf("%d", n);
			n++;
	}
}
