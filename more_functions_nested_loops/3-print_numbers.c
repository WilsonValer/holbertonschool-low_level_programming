#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * print_numbers - prints an integers
 *@n: number to print
 * Return: Always return 0
 */
void print_numbers(void)
{
	int n = 0;

	while (n < 10)

	{
		printf("%d", n);
			n++;
	}
}
