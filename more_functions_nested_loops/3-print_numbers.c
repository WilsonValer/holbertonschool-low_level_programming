#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * print_numbers - prints an integers
 *
 * Return: Always return 0
 */
void print_numbers(void)
{
	int n;

	n = 0;
	while (n < 10)

	{
		printf("%d", n);
			n++;
	}
	printf("\n");
}
