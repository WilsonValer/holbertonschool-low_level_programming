#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_puts - function that prints a string, followed by a new line, to stdout
 *@str: variable received how pointer
 * Return: return the string r
 */
void _puts(char *str)
{
	int i;
	int len;

	for (i = 0; i < len; i++)

		_putchar(str[i];
}
