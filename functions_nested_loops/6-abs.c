#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * _abs - return the absolute value of an number
 *
 * @n: Number recevied
 *
 * Return: return the absolute value of number
 */
int _abs(int n)
{
	int  m;

	m = abs(n);

	return (m);
}
